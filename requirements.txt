# Image AI Studio Backend — Python dependencies
# Generated from the imports used across app/. Requires Python 3.10+ (3.11/3.12 recommended).
# Install with:  pip install -r requirements.txt
# After installing, lock exact versions with:  pip freeze > requirements.lock.txt

# --- Web framework + ASGI server ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0      # ASGI server to run app.main:app (not imported, but required to serve)

# --- Data validation / settings ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0       # app/config.py loads settings from .env

# --- MongoDB (async) ---
motor>=3.5,<4.0                   # async driver; pulls in pymongo + bson
pymongo>=4.8,<5.0                 # used directly in app code

# --- Auth ---
bcrypt>=4.1,<5.0                  # password hashing
python-jose[cryptography]>=3.3,<4.0   # JWT encode/decode (from jose import jwt)
google-auth>=2.30,<3.0            # verify Google sign-in ID tokens (google.oauth2.id_token)

# --- HTTP client ---
httpx>=0.27,<1.0                  # calls to Gemini, Cloudinary, and Google APIs
